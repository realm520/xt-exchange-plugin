{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "[ -f \"${CLAUDE_PLUGIN_ROOT}/.venv/bin/python\" ] || bash \"${CLAUDE_PLUGIN_ROOT}/setup.sh\" 2>/dev/null"
          }
        ]
      }
    ]
  }
}
